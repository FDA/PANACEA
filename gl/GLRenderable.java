package com.eng.cber.na.gl;

import javax.media.opengl.GLAutoDrawable;

/**
 * An interface for objects that can be rendered by OpenGL.
 *
 */
public interface GLRenderable {
	public void render(GLAutoDrawable glAutoDrawable);
}
